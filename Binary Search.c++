#include <iostream>
using namespace std;
void binary(arr,tar){
    while(True){
        int size=sizeof(arr)
        int star=arr[0],end=arr[size-1]
        mid=star+end/2;
        
    }
}
int main(){
    arr={1,2,3,4,5,6,7,8,9}
    tar=0
    cout<<"Enter The Number to find:";
    cin>>tar;
    binary(arr,tar);
}
